using System;

namespace Arrays
{
    class Program
    {
        static void Main(string[] args)
        {
            for (int i = (n - 1); i >= 0 ; i--)
            {
                Console.Write("{0}" + " ", arr[i]);
            }
        }
    }
}
